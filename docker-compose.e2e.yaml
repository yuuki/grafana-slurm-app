services:
  grafana:
    environment:
      - GF_LOG_LEVEL=warn
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_AUTH_ANONYMOUS_ENABLED=false

  mysql:
    volumes:
      - ./e2e/initdb/03_e2e_seed.sql:/docker-entrypoint-initdb.d/03_e2e_seed.sql:ro
